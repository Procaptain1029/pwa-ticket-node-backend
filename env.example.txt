# ===========================================
# DISTRIMIA PWA - BACKEND ENVIRONMENT VARIABLES
# ===========================================
# Rename this file to .env and fill in the values

# Server Configuration
PORT=3001
NODE_ENV=development

# Supabase Configuration
# Get these from: Supabase Dashboard > Project Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI Configuration
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4-turbo-preview
# Vision model for /api/tickets/analyze-images (must support images)
OPENAI_VISION_MODEL=gpt-4o
# Optional: timeout (ms), retries, vision concurrency
# OPENAI_TIMEOUT_MS=120000
# OPENAI_MAX_RETRIES=2
# OPENAI_VISION_RETRY_ATTEMPTS=3
# OPENAI_VISION_CONCURRENCY=2

# JWT Configuration (for additional validation)
# Generate a random 32+ character string
JWT_SECRET=your-super-secret-jwt-key-min-32-characters

# CORS Configuration
# URL where your frontend is running
FRONTEND_URL=http://localhost:3000

# Lock Configuration
# How long a ticket lock lasts before auto-release (in minutes)
LOCK_TIMEOUT_MINUTES=10
